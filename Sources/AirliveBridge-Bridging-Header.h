// AirliveBridge-Bridging-Header.h — expose ObjC(++) types to Swift.
//
// AirPlayEngine is the AirPlay/Screen-Mirroring receiver (ObjC++ wrapping the
// vendored UxPlay lib); the Swift `AirPlayReceiver` drives it.

#import "AirPlayEngine.h"
